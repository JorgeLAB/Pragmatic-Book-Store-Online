<p id="notice"><%= notice %></p>

<h1>Add Quantity To Line Items</h1>

<table>
  <thead>
    <tr>
      <th>Quantity</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @add_quantity_to_line_items.each do |add_quantity_to_line_item| %>
      <tr>
        <td><%= add_quantity_to_line_item.quantity %></td>
        <td><%= link_to 'Show', add_quantity_to_line_item %></td>
        <td><%= link_to 'Edit', edit_add_quantity_to_line_item_path(add_quantity_to_line_item) %></td>
        <td><%= link_to 'Destroy', add_quantity_to_line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Add Quantity To Line Item', new_add_quantity_to_line_item_path %>
