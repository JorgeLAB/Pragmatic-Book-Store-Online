%p#notice= notice
%h1 Add Quantity To Line Items
%table
  %thead
    %tr
      %th Quantity
      %th{:colspan => "3"}
  %tbody
    - @add_quantity_to_line_items.each do |add_quantity_to_line_item|
      %tr
        %td= add_quantity_to_line_item.quantity
        %td= link_to 'Show', add_quantity_to_line_item
        %td= link_to 'Edit', edit_add_quantity_to_line_item_path(add_quantity_to_line_item)
        %td= link_to 'Destroy', add_quantity_to_line_item, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Add Quantity To Line Item', new_add_quantity_to_line_item_path
